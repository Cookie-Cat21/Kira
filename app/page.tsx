import KiraExperience from "@/app/components/KiraExperience";

export const metadata = {
  title: "Kira — Kapruka AI Shopping Concierge",
  description:
    "Send gifts across Sri Lanka with Kira — live Kapruka catalog, real delivery quotes, and checkout in one conversation.",
};

export default function HomePage() {
  return <KiraExperience />;
}
